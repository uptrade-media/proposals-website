import Files from './Files'

// Files route: render the full-featured Files component with image previews and custom folders
const FilesDrive = () => {
  return <Files />
}

export default FilesDrive
